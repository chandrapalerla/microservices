# Order-service has no additional secrets beyond the shared mysql + gateway secrets.
# This file is a placeholder — add order-specific secrets here if needed.
# (mysql-secret and gateway-secret are declared in infrastructure and api-gateway respectively)
apiVersion: v1
kind: Secret
metadata:
  name: order-service-secret
  namespace: ecommerce
type: Opaque
data: {}
