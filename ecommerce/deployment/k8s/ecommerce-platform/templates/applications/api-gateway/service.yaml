apiVersion: v1
kind: Service
metadata:
  name: apigateway
  namespace: ecommerce
  labels:
    app: apigateway
spec:
  type: NodePort
  selector:
    app: apigateway
  ports:
    - name: http
      port: 2027
      targetPort: 2027
      nodePort: 30027
