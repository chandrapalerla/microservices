# root-password = root123  (echo -n "root123" | base64)
# CHANGE IN PROD: use a dedicated per-service user with minimal grants.
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: ecommerce
type: Opaque
data:
  root-password: cm9vdDEyMw==    # root123 — [CHANGE IN PROD]
  username: cm9vdA==             # root    — [CHANGE IN PROD]
  password: cm9vdDEyMw==        # root123 — [CHANGE IN PROD]
