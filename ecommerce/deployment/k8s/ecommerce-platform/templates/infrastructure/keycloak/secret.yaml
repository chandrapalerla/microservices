# admin-password = admin123  (echo -n "admin123" | base64)
# CHANGE IN PROD: use a strong random password.
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-secret
  namespace: ecommerce
type: Opaque
data:
  admin-password: YWRtaW4xMjM=    # admin123 — [CHANGE IN PROD]
