apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: user-service
  labels:
    app: user-service
spec:
  type: LoadBalancer
  ports:
    - port: 2026
      targetPort: 2026
      protocol: TCP
      name: http
  selector:
    app: user-service

